.attachment id="attachment-#{file.id}"
  = link_to file.filename.to_s, url_for(file)
  - if current_user&.author?(file.record)
    = link_to 'Delete', active_storage_attachment_path(file), method: :delete, data: { confirm: 'Are you sure?', remote: true }