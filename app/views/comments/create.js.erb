<% if comment.persisted? %>
  var resource1 = $('#<%= commentable.class.to_s.downcase %>-<%= commentable.id %>')
  $(resource1).find('.comments').append('<%= j render comment %>');
  $(resource1).find('#comment_body').val('');
<% end %>