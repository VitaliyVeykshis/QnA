<% if comment.persisted? %>
  var resource = $('#<%= commentable.class.to_s.downcase %>-<%= commentable.id %>')
  $(resource).find('.comments').append('<%= j render comment %>');
  $(resource).find('.comments').removeClass('d-none');
  $(resource).find('#comment_body').val('');
<% end %>