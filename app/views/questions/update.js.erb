$('.edit-question .question-errors').html('<%= render 'shared/errors', resource: question %>');

<% if !question.errors.present? %>
  $('.question').html('<%= j render 'expanded_question', question: question %>');
<% end %>